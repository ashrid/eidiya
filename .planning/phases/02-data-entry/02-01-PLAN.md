---
phase: 02-data-entry
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/modules/validation/contributor.js
  - src/modules/validation/contributor.test.js
autonomous: true
requirements:
  - VAL-01
must_haves:
  truths:
    - Denomination breakdown sum validation works correctly
    - Validation returns clear error messages with actual vs expected values
    - All calculations use integer fils to prevent floating-point errors
  artifacts:
    - path: src/modules/validation/contributor.js
      provides: Denomination validation functions
      exports: ["calculateBreakdownTotal", "validateDenominationSum", "DENOMINATION_FILS"]
    - path: src/modules/validation/contributor.test.js
      provides: Unit tests for validation logic
      min_tests: 6
  key_links:
    - from: calculateBreakdownTotal
      to: DENOMINATION_FILS
      via: integer multiplication
    - from: validateDenominationSum
      to: calculateBreakdownTotal
      via: function call
---

<objective>
Create validation utilities for contributor form data, specifically denomination breakdown sum validation (VAL-01). This Wave 0 plan establishes the validation logic and unit tests that the form component will depend on.

Purpose: Ensure denomination breakdown sums equal the contribution amount with accurate integer-based calculations.
Output: Validation module with comprehensive unit tests.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-entry/02-CONTEXT.md
@.planning/phases/02-data-entry/02-RESEARCH.md
@.planning/phases/02-data-entry/02-VALIDATION.md

## Existing Code Patterns

From src/modules/state/schema.js:
- Denomination keys: five, ten, twenty, fifty, hundred, twoHundred, fiveHundred, thousand
- Validation returns { valid: boolean, error?: string } pattern
- Integer-based calculations (amountInFils)

From src/modules/money/Money.js:
- All money stored as integer fils (1 AED = 100 fils)
- Denominations in fils: 5 AED = 500 fils, 10 AED = 1000 fils, etc.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create denomination validation module</name>
  <files>src/modules/validation/contributor.js, src/modules/validation/contributor.test.js</files>
  <behavior>
    - Test 1: calculateBreakdownTotal returns 0 for empty breakdown
    - Test 2: calculateBreakdownTotal correctly sums single denomination
    - Test 3: calculateBreakdownTotal correctly sums multiple denominations
    - Test 4: validateDenominationSum returns valid=true when sums match
    - Test 5: validateDenominationSum returns valid=false with error when sums mismatch
    - Test 6: validateDenominationSum handles negative difference correctly
    - Test 7: All calculations use integer fils (no floating-point)
  </behavior>
  <action>
Create src/modules/validation/contributor.js with:
1. DENOMINATION_FILS constant mapping denomination keys to fils values:
   - five: 500, ten: 1000, twenty: 2000, fifty: 5000
   - hundred: 10000, twoHundred: 20000, fiveHundred: 50000, thousand: 100000
2. calculateBreakdownTotal(breakdown) - sums all denominations to total fils
3. validateDenominationSum(totalFils, breakdown) - compares breakdown sum to total
   - Returns { valid: boolean, error?: string }
   - Error message format: "Breakdown sum is {calculated} AED, but total is {total} AED (difference: {diff} AED)"
   - All amounts displayed in AED (divide fils by 100)

Create src/modules/validation/contributor.test.js with comprehensive tests following existing test patterns from Money.test.js and schema.test.js.

Follow existing code patterns:
- Use JSDoc comments
- Export functions as named exports
- Use parseInt(value, 10) || 0 for safe number parsing
- All internal calculations in integer fils
  </action>
  <verify>
    <automated>npm test -- contributor.test.js -x</automated>
  </verify>
  <done>All 7 validation tests pass, module exports correct functions, error messages include actual/expected/difference values</done>
</task>

</tasks>

<verification>
- [ ] DENOMINATION_FILS constant correctly maps all 8 denominations to fils
- [ ] calculateBreakdownTotal handles empty/null values gracefully
- [ ] validateDenominationSum returns detailed error messages per user decision
- [ ] All tests pass with `npm test -- contributor.test.js`
- [ ] Module follows existing code patterns (JSDoc, exports, error handling)
</verification>

<success_criteria>
1. Validation module exists with calculateBreakdownTotal and validateDenominationSum functions
2. Unit tests cover edge cases (empty breakdown, matching sums, mismatched sums)
3. Error messages show actual breakdown sum, expected total, and difference in AED
4. All calculations use integer fils internally
5. Tests pass and provide fast feedback (< 5 seconds)
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-entry/02-01-SUMMARY.md`
</output>
