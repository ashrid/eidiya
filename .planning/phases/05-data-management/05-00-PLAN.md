---
phase: 05-data-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/components/DataManager.test.js
  - src/modules/theme/ThemeManager.js
  - src/modules/theme/ThemeManager.test.js
  - src/components/ThemeToggle.test.js
autonomous: true
requirements:
  - PERS-04
  - PERS-05
  - UX-04
must_haves:
  truths:
    - DataManager tests exist covering export and import behaviors
    - ThemeManager module exists with get/set/toggle/init methods
    - ThemeManager tests exist covering theme switching and persistence
    - ThemeToggle component tests exist
  artifacts:
    - path: src/components/DataManager.test.js
      provides: Test scaffolding for DataManager component
      min_lines: 100
    - path: src/modules/theme/ThemeManager.js
      provides: Theme management module with persistence
      exports: ["ThemeManager"]
    - path: src/modules/theme/ThemeManager.test.js
      provides: Unit tests for theme management
      min_lines: 80
    - path: src/components/ThemeToggle.test.js
      provides: Component tests for theme toggle UI
      min_lines: 60
  key_links:
    - from: ThemeManager.test.js
      to: ThemeManager.js
      via: import
    - from: ThemeToggle.test.js
      to: ThemeManager.js
      via: import (mocked)
---

<objective>
Create test scaffolding for Data Management features (export/import and theme toggle) to enable TDD workflow for subsequent implementation plans.

Purpose: Establish test files that define expected behaviors before implementation, per Nyquist validation requirements in 05-VALIDATION.md.
Output: Four test/module files with failing tests ready for implementation.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-data-management/05-RESEARCH.md
@.planning/phases/05-data-management/05-VALIDATION.md

## Existing Patterns from Codebase

From src/modules/state/Store.js:
```javascript
export class Store {
  constructor(initialState = null, storage = null) {
    this._state = initialState || { ...DEFAULT_STATE };
    this._storage = storage;
    this._listeners = new Set();
  }
  getState() { return JSON.parse(JSON.stringify(this._state)); }
  setState(updater) { /* merges and notifies */ }
}
```

From src/modules/state/schema.js:
```javascript
export function validateState(data) {
  if (typeof data !== 'object' || data === null || Array.isArray(data)) {
    return { valid: false, error: 'state must be an object' };
  }
  // ... validation logic
  return { valid: true };
}
export const CURRENT_SCHEMA_VERSION = '1.1.0';
```

From src/modules/storage/SafeStorage.js:
```javascript
export class SafeStorage {
  setItem(key, value) { /* returns {success, fallback, error?} */ }
  getItem(key) { /* returns parsed value or null */ }
}
```

## Test Patterns from Existing Tests

From Store.test.js:
```javascript
// MockStorage pattern for tests
class MockStorage {
  constructor() { this.data = new Map(); }
  setItem(key, value) { this.data.set(key, value); return { success: true }; }
  getItem(key) { return this.data.get(key) || null; }
}

// Test structure
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
describe('ComponentName', () => {
  beforeEach(() => { /* setup */ });
  afterEach(() => { vi.restoreAllMocks(); });
});
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DataManager test scaffolding</name>
  <files>src/components/DataManager.test.js</files>
  <behavior>
    - Test exportData: returns success true, generates filename with date pattern
    - Test exportData: includes metadata (exportedAt, appVersion) in export
    - Test importData: parses valid JSON file and validates against schema
    - Test importData: rejects invalid JSON with appropriate error
    - Test importData: rejects data failing schema validation
    - Test importData: shows confirmation when data exists (has contributors)
    - Test importData: applies valid data to store via setState
  </behavior>
  <action>
    Create src/components/DataManager.test.js with comprehensive test cases for DataManager component. Use MockStorage pattern from Store.test.js. Mock FileReader for import tests. Tests should fail initially (DataManager not yet implemented). Export filename pattern: `eidiya-backup-YYYY-MM-DD.json`. Import validation should use existing validateState() from schema.js.
  </action>
  <verify>
    <automated>npm test -- DataManager.test.js 2>&1 | grep -E "(FAIL|failed|passed)" | head -5</automated>
  </verify>
  <done>Test file exists with 7+ test cases covering export and import behaviors, all failing as expected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ThemeManager module and tests</name>
  <files>src/modules/theme/ThemeManager.js, src/modules/theme/ThemeManager.test.js</files>
  <behavior>
    - Test get(): returns stored theme preference from localStorage
    - Test get(): returns 'dark' when system prefers dark scheme and no stored preference
    - Test get(): returns 'light' when system prefers light scheme and no stored preference
    - Test set(theme): sets data-theme attribute on document.documentElement
    - Test set(theme): persists theme to localStorage
    - Test toggle(): switches from light to dark
    - Test toggle(): switches from dark to light
    - Test init(): applies stored preference on page load
  </behavior>
  <action>
    Create src/modules/theme/ThemeManager.js as a singleton object (not class) with methods: get(), set(theme), toggle(), init(). Use localStorage directly (not SafeStorage - theme is non-critical). STORAGE_KEY = 'eidiya:theme'. Check system preference via window.matchMedia('(prefers-color-scheme: dark)').

    Create src/modules/theme/ThemeManager.test.js mocking localStorage and matchMedia. Tests should initially fail (module skeleton only).
  </action>
  <verify>
    <automated>npm test -- ThemeManager.test.js 2>&1 | grep -E "(FAIL|failed|passed)" | head -5</automated>
  </verify>
  <done>ThemeManager.js skeleton exists with all methods, ThemeManager.test.js has 8+ test cases, all failing as expected</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ThemeToggle component tests</name>
  <files>src/components/ThemeToggle.test.js</files>
  <behavior>
    - Test render(): creates button element with correct aria-label
    - Test render(): displays sun icon (☀️) when in dark mode
    - Test render(): displays moon icon (🌙) when in light mode
    - Test click: toggles theme via ThemeManager.toggle()
    - Test click: updates icon after toggle
  </behavior>
  <action>
    Create src/components/ThemeToggle.test.js with tests for ThemeToggle component. Mock ThemeManager module. Component should be a class with render() method returning a button element. Icons: sun (☀️) for dark mode (click to go light), moon (🌙) for light mode (click to go dark). Tests should fail initially (component not yet implemented).
  </action>
  <verify>
    <automated>npm test -- ThemeToggle.test.js 2>&1 | grep -E "(FAIL|failed|passed)" | head -5</automated>
  </verify>
  <done>Test file exists with 5+ test cases, all failing as expected</done>
</task>

</tasks>

<verification>
- All three test files exist with comprehensive test coverage
- Running `npm test` shows expected failures for unimplemented features
- Test patterns follow existing codebase conventions (vitest, jsdom environment)
- No implementation code yet - tests define expected behavior
</verification>

<success_criteria>
- src/components/DataManager.test.js exists with 7+ tests (export + import)
- src/modules/theme/ThemeManager.js skeleton exists with method stubs
- src/modules/theme/ThemeManager.test.js exists with 8+ tests
- src/components/ThemeToggle.test.js exists with 5+ tests
- All tests fail as expected (RED phase of TDD)
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-management/05-00-SUMMARY.md`
</output>
