---
phase: 05-data-management
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/modules/theme/ThemeManager.js
  - src/modules/theme/ThemeManager.test.js
  - src/components/ThemeToggle.js
  - src/components/ThemeToggle.test.js
  - src/components/AppContainer.js
  - index.html
  - src/styles/main.css
autonomous: true
requirements:
  - UX-04
must_haves:
  truths:
    - Theme toggle button switches between light and dark modes
    - Theme preference persists across page reloads
    - Theme respects system preference when no stored preference
    - No flash of wrong theme on page load
    - Toggle button shows appropriate icon for current mode
  artifacts:
    - path: src/modules/theme/ThemeManager.js
      provides: Complete theme management module
      exports: ["ThemeManager"]
      contains: ["get()", "set()", "toggle()", "init()"]
    - path: src/components/ThemeToggle.js
      provides: Theme toggle UI component
      exports: ["ThemeToggle"]
      contains: ["render()", "_getIcon()"]
    - path: index.html
      provides: Early theme initialization
      contains: "data-theme" attribute update script
  key_links:
    - from: ThemeToggle
      to: ThemeManager
      via: import and method calls
      pattern: "ThemeManager.toggle()"
    - from: index.html
      to: ThemeManager
      via: inline script (early init)
      pattern: "localStorage.getItem('eidiya:theme')"
    - from: ThemeManager
      to: document.documentElement
      via: data-theme attribute
      pattern: "setAttribute('data-theme', theme)"
---

<objective>
Implement dark mode support with theme toggle, system preference detection, and persistence.

Purpose: Fulfill UX-04 requirement - app supports light/dark modes with persistent preference.
Output: Working theme toggle in header, smooth theme switching, no flash on load.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-data-management/05-RESEARCH.md
@.planning/phases/05-data-management/05-02-SUMMARY.md

## Pico CSS Dark Mode Pattern
From research:
```javascript
const ThemeManager = {
  STORAGE_KEY: 'eidiya:theme',

  get() {
    const stored = localStorage.getItem(this.STORAGE_KEY);
    if (stored === 'dark' || stored === 'light') return stored;
    return window.matchMedia('(prefers-color-scheme: dark)').matches
      ? 'dark' : 'light';
  },

  set(theme) {
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem(this.STORAGE_KEY, theme);
  },

  toggle() {
    const current = this.get();
    this.set(current === 'dark' ? 'light' : 'dark');
  },

  init() {
    this.set(this.get());
  }
};
```

## Early Theme Initialization (Prevents Flash)
Add to index.html <head>:
```html
<script>
  (function() {
    const stored = localStorage.getItem('eidiya:theme');
    const theme = stored || (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
    document.documentElement.setAttribute('data-theme', theme);
  })();
</script>
```

## Existing index.html Structure
```html
<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eidiya - Eid Money Tracker</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  <link rel="stylesheet" href="/src/styles/main.css">
</head>
```

## Theme Toggle CSS Pattern
```css
.theme-toggle {
  position: fixed;
  top: var(--spacing-md);
  right: var(--spacing-md);
  background: var(--pico-card-background-color);
  border: 1px solid var(--pico-muted-border-color);
  border-radius: var(--pico-border-radius);
  padding: var(--spacing-sm);
  cursor: pointer;
  font-size: 1.25rem;
  z-index: 1000;
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete ThemeManager implementation</name>
  <files>src/modules/theme/ThemeManager.js</files>
  <behavior>
    - get(): reads from localStorage, validates 'dark' or 'light', falls back to matchMedia
    - set(theme): sets data-theme on document.documentElement, saves to localStorage
    - toggle(): gets current, switches to opposite, calls set()
    - init(): calls set(get()) to apply stored/system preference on load
    - STORAGE_KEY = 'eidiya:theme'
  </behavior>
  <action>
    Implement complete ThemeManager module in src/modules/theme/ThemeManager.js. Use localStorage directly (theme preference is non-critical data). Check system preference with window.matchMedia('(prefers-color-scheme: dark)').matches. Module should be a singleton object (not class) for simplicity.
  </action>
  <verify>
    <automated>npm test -- ThemeManager.test.js -x 2>&1 | tail -5</automated>
  </verify>
  <done>ThemeManager fully implemented, all 8 tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ThemeToggle component</name>
  <files>src/components/ThemeToggle.js</files>
  <behavior>
    - render() creates button with aria-label="Toggle dark mode"
    - _getIcon() returns '☀️' when in dark mode (suggests switching to light)
    - _getIcon() returns '🌙' when in light mode (suggests switching to dark)
    - Click handler calls ThemeManager.toggle() and updates button icon
    - Button uses CSS class 'theme-toggle' for styling
  </behavior>
  <action>
    Implement src/components/ThemeToggle.js as a class component. Import ThemeManager. render() returns button element with click listener. Icon updates reflect new state after toggle. Follow existing component patterns (constructor, render, destroy if needed).
  </action>
  <verify>
    <automated>npm test -- ThemeToggle.test.js -x 2>&1 | tail -5</automated>
  </verify>
  <done>ThemeToggle component implemented, all 5 tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add early theme initialization to index.html</name>
  <files>index.html</files>
  <action>
    Add inline script to index.html <head> BEFORE any stylesheets load:
    ```html
    <script>
      (function() {
        const stored = localStorage.getItem('eidiya:theme');
        const theme = stored === 'dark' || stored === 'light'
          ? stored
          : (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        document.documentElement.setAttribute('data-theme', theme);
      })();
    </script>
    ```
    This prevents theme flash before JavaScript loads.
  </action>
  <verify>
    <automated>grep -A5 "eidiya:theme" index.html | head -7</automated>
  </verify>
  <done>Early theme script added to index.html head</done>
</task>

<task type="auto">
  <name>Task 4: Add theme toggle styles</name>
  <files>src/styles/main.css</files>
  <action>
    Add CSS for theme toggle button to src/styles/main.css:
    - .theme-toggle: fixed position top-right, z-index 1000
    - Use Pico CSS variables for theming (--pico-card-background-color, etc.)
    - Ensure button is visible and clickable on mobile (min 44x44px touch target)
    - Add hover state
    - Position: fixed top: 1rem right: 1rem
  </action>
  <verify>
    <automated>grep -n "theme-toggle" src/styles/main.css | head -5</automated>
  </verify>
  <done>Theme toggle styles added with proper positioning and theming</done>
</task>

<task type="auto">
  <name>Task 5: Integrate ThemeToggle into AppContainer</name>
  <files>src/components/AppContainer.js</files>
  <action>
    Import ThemeToggle in AppContainer.js. Instantiate and render in header area or fixed position. Add to destroy() method for cleanup. Theme toggle should be visible on all views.
  </action>
  <verify>
    <automated>grep -n "ThemeToggle" src/components/AppContainer.js | head -5</automated>
  </verify>
  <done>ThemeToggle integrated into AppContainer, visible in UI</done>
</task>

</tasks>

<verification>
- `npm test -- ThemeManager.test.js` passes all tests
- `npm test -- ThemeToggle.test.js` passes all tests
- `npm test` full suite passes
- Theme toggle button visible in top-right corner
- Clicking toggle switches between light/dark modes
- Theme persists after page reload
- No flash of wrong theme on load (early script works)
</verification>

<success_criteria>
- ThemeManager module fully functional with get/set/toggle/init
- ThemeToggle component renders and switches themes
- Early theme script in index.html prevents flash
- Theme persists in localStorage
- Respects system preference when no stored preference
- Toggle button shows appropriate icon (sun/moon)
- All theme tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-management/05-03-SUMMARY.md`
</output>
