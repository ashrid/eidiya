---
phase: 04-distribution
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/ContributorCard.js
  - src/components/DistributionPanel.js
  - src/components/AppContainer.js
  - src/styles/main.css
autonomous: true
requirements:
  - DIST-01
  - DIST-02
must_haves:
  truths:
    - Each contributor card has a "Received" checkbox toggle
    - Checking/unchecking updates the contributor's received status
    - Received contributors show visual indication (dimmed/strikethrough)
    - Distribution panel shows progress (X of Y received)
    - Distribution panel shows remaining notes table
    - Distribution panel updates live when received status changes
  artifacts:
    - path: src/components/ContributorCard.js
      provides: Received toggle UI in each card
      contains: input[type="checkbox"], received-section, received class
    - path: src/components/DistributionPanel.js
      provides: Distribution tracking panel
      exports: ["DistributionPanel"]
    - path: src/components/AppContainer.js
      provides: Integration of DistributionPanel
      contains: new DistributionPanel, panel.subscribe()
    - path: src/styles/main.css
      provides: Styling for received state and distribution panel
      contains: .received-section, .contributor-card.received, .distribution-panel
  key_links:
    - from: ContributorCard checkbox
      to: Store.updateContributor
      via: onUpdate callback with { received: boolean }
      pattern: "change.*received.*target.checked"
    - from: DistributionPanel
      to: store.subscribe
      via: subscribe() method
      pattern: "this._store.subscribe"
    - from: DistributionPanel render
      to: calculateRemainingNotes
      via: import and function call
      pattern: "calculateRemainingNotes.*contributors"
---

<objective>
Build the distribution tracking UI with received toggles and remaining notes display.

Purpose: Allow users to mark contributors as received and see what's left to distribute.
Output: Updated ContributorCard with received toggle, new DistributionPanel component, and integration.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-distribution/04-RESEARCH.md
@src/components/ContributorCard.js
@src/components/SummaryPanel.js
@src/components/AppContainer.js
@src/modules/state/selectors.js
@src/styles/main.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add received toggle to ContributorCard</name>
  <files>src/components/ContributorCard.js</files>
  <behavior>
    - Render adds a "received-section" with checkbox and label
    - Checkbox id is unique per contributor: `received-${id}`
    - Checkbox checked state reflects contributor.received (default false)
    - Label text is "Received"
    - Change event calls onUpdate with { received: checked }
    - Card has 'received' class when contributor.received is true (for styling)
    - Visual indication: received cards show strikethrough on name and reduced opacity
    - Toggle is always visible (not hidden during inline edit)
  </behavior>
  <action>
    Modify src/components/ContributorCard.js:
    1. In render(), after breakdownSection, create receivedSection div
    2. Create checkbox input with type="checkbox", id=`received-${this._contributor.id}`
    3. Set checkbox.checked = this._contributor.received || false
    4. Add change event listener that calls this._onUpdate(this._contributor.id, { received: e.target.checked })
    5. Create label with htmlFor matching checkbox id, textContent "Received"
    6. Add 'received' class to card element when this._contributor.received is true
    7. Append checkbox and label to receivedSection, append receivedSection to card
    8. Add CSS classes for styling hook
  </action>
  <verify>
    <automated>npm test -- src/components/ContributorCard.test.js --reporter=dot</automated>
  </verify>
  <done>ContributorCard renders received checkbox; toggling calls onUpdate; visual state changes based on received status</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DistributionPanel component</name>
  <files>src/components/DistributionPanel.js</files>
  <behavior>
    - Constructor accepts store parameter
    - render() returns aside element with class "distribution-panel"
    - Panel shows header "Distribution"
    - Progress section shows "X of Y received (Z%)" format
    - Progress bar visual (optional but nice)
    - Remaining notes table shows denomination and count for unreceived contributors
    - Table only shows denominations with count > 0
    - Empty state message when all contributors received or no contributors
    - subscribe() method attaches to store and re-renders on changes
    - destroy() method cleans up subscription
  </behavior>
  <action>
    Create src/components/DistributionPanel.js following SummaryPanel pattern:
    1. Import formatAED from '@modules/money/formatters.js'
    2. Import calculateRemainingNotes and calculateDistributionProgress from '@modules/state/selectors.js'
    3. Export class DistributionPanel with constructor(store), render(), subscribe(), unsubscribe(), destroy(), getElement()
    4. render() creates aside.distribution-panel with:
       - Header with h2 "Distribution"
       - Progress section using calculateDistributionProgress
       - Remaining notes table using calculateRemainingNotes (only denominations > 0)
       - Empty state when appropriate
    5. subscribe() attaches to store.subscribe and calls _rerender on changes
    6. Use flash animation on update (copy from SummaryPanel)
  </action>
  <verify>
    <automated>npm test -- src/components/DistributionPanel.test.js --reporter=dot</automated>
  </verify>
  <done>DistributionPanel renders with progress stats and remaining notes; subscribes to store updates</done>
</task>

<task type="auto">
  <name>Task 3: Integrate DistributionPanel and add CSS styles</name>
  <files>src/components/AppContainer.js, src/styles/main.css</files>
  <behavior>
    - AppContainer creates DistributionPanel instance
    - DistributionPanel is rendered in the sidebar area (similar to SummaryPanel)
    - DistributionPanel.subscribe() is called after rendering
    - DistributionPanel.destroy() is called in AppContainer.destroy()
    - CSS styles for received toggle section
    - CSS styles for received card state (dimmed, strikethrough)
    - CSS styles for distribution panel (similar to summary-panel)
    - Distribution panel appears below SummaryPanel in sidebar
  </behavior>
  <action>
    1. Modify src/components/AppContainer.js:
       - Import DistributionPanel
       - Create this._distributionPanel in constructor
       - Render distribution panel in _renderSidebar() after summary panel
       - Call this._distributionPanel.subscribe() after rendering
       - Call this._distributionPanel.destroy() in destroy()

    2. Add to src/styles/main.css:
       - .received-section styling (flex layout, border-top, spacing)
       - .received-section input[type="checkbox"] sizing
       - .contributor-card.received styling (opacity: 0.7, text-decoration: line-through on name)
       - .distribution-panel styling (similar to summary-panel)
       - .distribution-panel .progress-bar styling
       - .distribution-panel .remaining-table styling
  </action>
  <verify>
    <automated>npm test -- --reporter=dot</automated>
  </verify>
  <done>DistributionPanel integrated in AppContainer; CSS styles added; all tests pass</done>
</task>

</tasks>

<verification>
- ContributorCard.test.js: Received toggle tests pass
- DistributionPanel.test.js: Panel rendering and subscription tests pass
- Manual verification: Toggle a contributor as received, verify DistributionPanel updates
- Visual check: Received cards show strikethrough and dimming
</verification>

<success_criteria>
- ContributorCard displays received checkbox that toggles contributor status
- Received contributors show visual distinction (dimmed, strikethrough name)
- DistributionPanel shows "X of Y received (Z%)" progress
- DistributionPanel displays remaining notes table (only unreceived contributors)
- Panel updates automatically when received status changes
- All tests pass (npm test)
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-02-SUMMARY.md`
</output>
