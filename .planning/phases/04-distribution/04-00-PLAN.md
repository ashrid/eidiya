---
phase: 04-distribution
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/modules/state/selectors.test.js
  - src/components/ContributorCard.test.js
  - src/components/DistributionPanel.test.js
  - src/components/DistributionPrintView.test.js
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
must_haves:
  truths:
    - Test files exist for all new distribution functionality
    - Tests follow existing Vitest patterns from codebase
    - Tests cover received toggle, remaining notes calculation, and print view
  artifacts:
    - path: src/modules/state/selectors.test.js
      provides: Extended tests for distribution selectors
      contains: calculateRemainingNotes, calculateDistributionProgress
    - path: src/components/DistributionPanel.test.js
      provides: Tests for distribution panel component
      contains: render, subscribe, remaining notes display
    - path: src/components/DistributionPrintView.test.js
      provides: Tests for print view component
      contains: render, per-person lists, print styling
    - path: src/components/ContributorCard.test.js
      provides: Extended tests for received toggle
      contains: checkbox, received state, visual indication
  key_links:
    - from: Test files
      to: Implementation files
      via: Import statements and mock data
      pattern: "import { X } from './X.js'"
---

<objective>
Create test scaffolding for Phase 4 distribution features before implementation.

Purpose: Establish test contracts that implementation must satisfy (TDD-style preparation).
Output: Four test files covering distribution selectors, panel, print view, and received toggle.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-distribution/04-RESEARCH.md
@.planning/phases/04-distribution/04-VALIDATION.md
@src/modules/state/selectors.js
@src/modules/state/selectors.test.js
@src/components/ContributorCard.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend selectors.test.js with distribution selectors</name>
  <files>src/modules/state/selectors.test.js</files>
  <behavior>
    - Test calculateRemainingNotes: returns zeroed object for empty/null/undefined input
    - Test calculateRemainingNotes: sums only unreceived contributors (received === false or undefined)
    - Test calculateRemainingNotes: excludes received contributors from totals
    - Test calculateRemainingNotes: handles missing breakdown fields gracefully
    - Test calculateDistributionProgress: returns zeros for empty input
    - Test calculateDistributionProgress: calculates total, received, remaining, percentComplete correctly
    - Test calculateDistributionProgress: handles 0 contributors (avoid division by zero)
    - Test calculateDistributionProgress: updates when contributors marked received
  </behavior>
  <action>
    Add test suites for calculateRemainingNotes and calculateDistributionProgress to src/modules/state/selectors.test.js.
    Follow existing test patterns (describe/it/expect) and use the mock contributor structure from existing tests.
    Import the new functions (they don't exist yet - tests will fail until Plan 01 implements them).
  </action>
  <verify>
    <automated>npm test -- src/modules/state/selectors.test.js --reporter=dot</automated>
  </verify>
  <done>Test file has comprehensive coverage for distribution selectors; tests fail (functions not yet implemented)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DistributionPanel.test.js</name>
  <files>src/components/DistributionPanel.test.js</files>
  <behavior>
    - Test constructor: accepts store parameter
    - Test render: returns HTMLElement with correct class
    - Test render: displays progress stats (X of Y received)
    - Test render: shows remaining notes table with correct counts
    - Test render: shows print button when contributors exist
    - Test subscribe: calls store.subscribe and re-renders on state change
    - Test unsubscribe: removes subscription
    - Test destroy: cleans up subscription and removes element
  </behavior>
  <action>
    Create src/components/DistributionPanel.test.js following the SummaryPanel.test.js pattern.
    Mock the store with getState() and subscribe() methods.
    Test that panel displays remaining notes calculated from store state.
  </action>
  <verify>
    <automated>npm test -- src/components/DistributionPanel.test.js --reporter=dot</automated>
  </verify>
  <done>DistributionPanel test file exists with comprehensive coverage</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create DistributionPrintView.test.js and extend ContributorCard.test.js</name>
  <files>src/components/DistributionPrintView.test.js, src/components/ContributorCard.test.js</files>
  <behavior>
    DistributionPrintView tests:
    - Test constructor: accepts contributors array
    - Test render: returns HTMLElement with print-view class
    - Test render: includes header with title and date
    - Test render: creates per-person distribution cards
    - Test render: each card shows name, amount, breakdown list
    - Test render: each card has blank checkbox for physical marking
    - Test render: handles empty contributors array gracefully

    ContributorCard extension tests:
    - Test render: shows received checkbox when contributor has received field
    - Test render: checkbox is checked when received is true
    - Test render: checkbox is unchecked when received is false/undefined
    - Test interaction: clicking checkbox calls onUpdate with received value
    - Test visual: card has 'received' class when contributor.received is true
  </behavior>
  <action>
    Create src/components/DistributionPrintView.test.js with tests for print-optimized view rendering.
    Extend src/components/ContributorCard.test.js with tests for received toggle functionality.
    Follow existing test patterns using vi.fn() for mocks and jsdom for DOM testing.
  </action>
  <verify>
    <automated>npm test -- src/components/DistributionPrintView.test.js src/components/ContributorCard.test.js --reporter=dot</automated>
  </verify>
  <done>Both test files exist; DistributionPrintView tests cover print layout, ContributorCard tests cover received toggle</done>
</task>

</tasks>

<verification>
- All Wave 0 test files exist and follow Vitest patterns
- Tests fail as expected (implementation not yet done)
- npm test -- --reporter=dot runs without configuration errors
</verification>

<success_criteria>
- src/modules/state/selectors.test.js extended with distribution selector tests
- src/components/DistributionPanel.test.js created
- src/components/DistributionPrintView.test.js created
- src/components/ContributorCard.test.js extended with received toggle tests
- All test files use proper vitest-environment jsdom annotation
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-00-SUMMARY.md`
</output>
