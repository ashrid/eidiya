---
phase: 04-distribution
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/components/DistributionPrintView.js
  - src/components/AppContainer.js
  - src/styles/main.css
autonomous: false
requirements:
  - DIST-03
must_haves:
  truths:
    - User can trigger print view from the UI
    - Print view shows per-person distribution lists
    - Each list shows name, amount, and note breakdown
    - Print view has blank checkboxes for physical marking
    - Print CSS hides all UI chrome (buttons, forms, menus)
    - Print layout is clean and readable on paper
  artifacts:
    - path: src/components/DistributionPrintView.js
      provides: Print-optimized view component
      exports: ["DistributionPrintView"]
    - path: src/styles/main.css
      provides: Print styles and screen-only hiding
      contains: "@media print", ".print-only", ".no-print"
    - path: src/components/AppContainer.js
      provides: Print button integration
      contains: "Print" button handler
  key_links:
    - from: Print button
      to: window.print()
      via: click event handler
      pattern: "window.print"
    - from: "@media print" CSS
      to: UI elements
      via: "display: none !important"
      pattern: ".no-print.*display.*none"
    - from: DistributionPrintView
      to: contributors data
      via: store.getState()
      pattern: "store.getState().contributors"
---

<objective>
Implement printable distribution lists for physical handout during Eid.

Purpose: Generate clean, readable printouts showing what notes each person should receive.
Output: DistributionPrintView component, print CSS, and print button integration.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-distribution/04-RESEARCH.md
@src/components/DistributionPrintView.test.js
@src/components/AppContainer.js
@src/styles/main.css
@src/modules/money/formatters.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DistributionPrintView component</name>
  <files>src/components/DistributionPrintView.js</files>
  <behavior>
    - Constructor accepts contributors array
    - render() returns div with class "print-view"
    - Header shows "Eidiya Distribution List" and current date
    - Creates per-person distribution cards
    - Each card shows: name (h3), amount (formatted AED), breakdown list, blank checkbox line
    - Breakdown list shows "X AED x count" for each denomination with count > 0
    - Cards are sorted alphabetically by name
    - Empty state if no contributors
  </behavior>
  <action>
    Create src/components/DistributionPrintView.js:
    1. Import formatAED from '@modules/money/formatters.js'
    2. Export class DistributionPrintView
    3. Constructor: this._contributors = contributors (sorted copy)
    4. render() method:
       - Create container div with class "print-view print-only"
       - Add header with h1 "Eidiya Distribution List" and date
       - Create distribution-lists container
       - For each contributor, call _renderPersonList and append
    5. _renderPersonList(contributor) private method:
       - Create div with class "distribution-card"
       - Add h3 with contributor.name
       - Add p.amount with formatAED(contributor.amountInFils)
       - Add ul with breakdown items (filter count > 0)
       - Add checkbox-line div with "Received:" label and "[ ]" checkbox placeholder
    6. getElement() method returns the rendered element
  </action>
  <verify>
    <automated>npm test -- src/components/DistributionPrintView.test.js --reporter=dot</automated>
  </verify>
  <done>DistributionPrintView renders per-person lists with name, amount, breakdown, and blank checkbox</done>
</task>

<task type="auto">
  <name>Task 2: Add print styles to CSS</name>
  <files>src/styles/main.css</files>
  <behavior>
    - @media print section hides non-printable elements
    - .no-print class hides elements in print
    - .print-only class shows elements only in print
    - Distribution cards have borders and proper spacing for paper
    - Page breaks avoided inside distribution cards
    - Background colors print correctly (-webkit-print-color-adjust)
  </behavior>
  <action>
    Extend src/styles/main.css:
    1. Add .print-only class (display: none on screen)
    2. Add .no-print class for elements to hide in print
    3. Extend @media print section:
       - Hide: .form-section, .menu-container, .action-menu, .storage-warning
       - Hide: button:not(.print-button), .summary-panel (or minimize it)
       - Hide: .received-section (the toggle - print view has its own checkbox)
       - Show: .print-only
       - Style .distribution-card: border, padding, margin, break-inside: avoid
       - Style .print-view header: centered, margin-bottom
       - Add * { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
       - Optimize for A4/Letter paper size
  </action>
  <verify>
    <automated>npm test -- --reporter=dot</automated>
  </verify>
  <done>Print styles added; @media print hides UI chrome and shows print-only elements</done>
</task>

<task type="auto">
  <name>Task 3: Integrate print button in AppContainer</name>
  <files>src/components/AppContainer.js</files>
  <behavior>
    - Import DistributionPrintView
    - Add "Print Distribution List" button to UI (in DistributionPanel or sidebar)
    - Button click triggers print workflow:
      1. Create DistributionPrintView with current contributors
      2. Append to body temporarily
      3. Call window.print()
      4. Remove print view from DOM after print dialog closes
    - Button is hidden when no contributors exist
  </behavior>
  <action>
    Modify src/components/AppContainer.js:
    1. Import DistributionPrintView from './DistributionPrintView.js'
    2. Add print button to DistributionPanel (or create separate print trigger)
    3. Create _handlePrint() method:
       - Get current contributors from store
       - Create new DistributionPrintView(contributors)
       - Render and append to document.body
       - Call window.print()
       - Use setTimeout to remove print view after print dialog (100ms delay)
    4. Alternative: Add print button directly in DistributionPanel component
    5. Ensure button has class for print CSS to hide it
  </action>
  <verify>
    <automated>npm test -- --reporter=dot</automated>
  </verify>
  <done>Print button integrated; clicking triggers window.print() with DistributionPrintView content</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Verify print output quality</name>
  <what-built>Print functionality with DistributionPrintView component and print-optimized CSS</what-built>
  <how-to-verify>
    1. Open the app in browser (npm run dev)
    2. Add at least 2-3 test contributors with different denomination breakdowns
    3. Mark one contributor as "received"
    4. Click "Print Distribution List" button
    5. In print preview, verify:
       - No UI buttons, forms, or menus are visible
       - Header shows "Eidiya Distribution List" and current date
       - Each contributor has their own card with name, amount, and breakdown
       - Breakdown shows correct note counts (e.g., "100 AED x 5")
       - Each card has a blank "Received: [ ]" checkbox line
       - Layout is clean with proper spacing and borders
       - Text is readable (not too small)
    6. Save as PDF and verify formatting is preserved
  </how-to-verify>
  <resume-signal>Type "approved" if print layout looks correct, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- DistributionPrintView.test.js: All tests pass
- Manual verification: Print preview shows clean layout without UI chrome
- Print preview: Each contributor has their own card with breakdown
- Print preview: Blank checkboxes present for physical marking
- CSS: @media print properly hides .no-print elements
</verification>

<success_criteria>
- DistributionPrintView component renders per-person distribution lists
- Print view includes header, date, and all contributors
- Each distribution card shows name, amount, breakdown, and blank checkbox
- Print CSS hides all UI chrome (buttons, forms, menus)
- Print button triggers browser print dialog
- Print layout is clean and readable on paper/PDF
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-03-SUMMARY.md`
</output>
