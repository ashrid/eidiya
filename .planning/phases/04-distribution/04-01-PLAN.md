---
phase: 04-distribution
plan: 01
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - src/modules/state/schema.js
  - src/modules/state/selectors.js
  - src/modules/state/Store.js
autonomous: true
requirements:
  - DIST-01
must_haves:
  truths:
    - Contributor schema includes received boolean field
    - Existing data migrates with received defaulting to false
    - calculateRemainingNotes selector filters received contributors
    - calculateDistributionProgress provides completion statistics
  artifacts:
    - path: src/modules/state/schema.js
      provides: Updated Contributor typedef and migration
      contains: "@property {boolean} [received]", "c.received ?? false"
    - path: src/modules/state/selectors.js
      provides: Distribution calculation functions
      exports: ["calculateRemainingNotes", "calculateDistributionProgress"]
    - path: src/modules/state/Store.js
      provides: Optional toggleReceived convenience method
      contains: toggleReceived method
  key_links:
    - from: migrateState
      to: contributor.received
      via: "c.received ?? false"
      pattern: "received: c.received ?? false"
    - from: calculateRemainingNotes
      to: contributor.received
      via: .filter(c => !c.received)
      pattern: "filter.*!c.received"
---

<objective>
Extend data layer to support distribution tracking with received status and remaining notes calculation.

Purpose: Enable the app to track who has received their exchanged notes and calculate what's left to distribute.
Output: Updated schema with received field, distribution selectors, and optional Store helper.
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
@/home/rashid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-distribution/04-RESEARCH.md
@src/modules/state/schema.js
@src/modules/state/selectors.js
@src/modules/state/Store.js
@src/modules/state/selectors.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend schema with received field and migration</name>
  <files>src/modules/state/schema.js</files>
  <behavior>
    - Update CURRENT_SCHEMA_VERSION to '1.1.0'
    - Add @property {boolean} [received] to Contributor typedef JSDoc
    - Update validateContributor to accept received as optional boolean
    - Update migrateState to default received to false for existing contributors
    - Default new contributors to received: false in DEFAULT_STATE pattern
  </behavior>
  <action>
    Modify src/modules/state/schema.js:
    1. Change CURRENT_SCHEMA_VERSION from '1.0.0' to '1.1.0'
    2. Add to Contributor typedef: @property {boolean} [received] - Whether contributor has received their notes
    3. In validateContributor, add optional validation for received field (must be boolean if present)
    4. In migrateState, map contributors with: received: c.received ?? false
    5. Ensure DEFAULT_STATE uses version '1.1.0'
  </action>
  <verify>
    <automated>npm test -- src/modules/state/schema.test.js --reporter=dot</automated>
  </verify>
  <done>Schema version is 1.1.0; received field documented and migrated; tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add distribution selectors</name>
  <files>src/modules/state/selectors.js</files>
  <behavior>
    - calculateRemainingNotes(contributors): Returns object with note counts for unreceived contributors only
    - calculateRemainingNotes: Handles null/undefined/empty input gracefully (returns zeros)
    - calculateRemainingNotes: Sums breakdown across all contributors where received !== true
    - calculateDistributionProgress(contributors): Returns { total, received, remaining, percentComplete }
    - calculateDistributionProgress: total = contributors.length
    - calculateDistributionProgress: received = count where received === true
    - calculateDistributionProgress: remaining = total - received
    - calculateDistributionProgress: percentComplete = Math.round((received / total) * 100) or 0 if total is 0
  </behavior>
  <action>
    Add to src/modules/state/selectors.js:
    1. calculateRemainingNotes function - filter contributors by !c.received, then reduce breakdowns
    2. calculateDistributionProgress function - count totals and calculate percentage
    Follow the same defensive coding patterns as calculateBankNotes (null checks, default zeros).
    Export both functions.
  </action>
  <verify>
    <automated>npm test -- src/modules/state/selectors.test.js --reporter=dot</automated>
  </verify>
  <done>Both selectors implemented and exported; all selector tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Add toggleReceived helper to Store</name>
  <files>src/modules/state/Store.js</files>
  <behavior>
    - Store.toggleReceived(id) finds contributor by ID
    - Toggles received boolean (true -> false, false -> true, undefined -> true)
    - Calls updateContributor with new received value
    - Returns updated contributor or null if not found
  </behavior>
  <action>
    Add toggleReceived method to Store class in src/modules/state/Store.js:
    ```javascript
    toggleReceived(id) {
      const state = this.getState();
      const contributor = state.contributors.find(c => c.id === id);
      if (!contributor) return null;
      return this.updateContributor(id, { received: !contributor.received });
    }
    ```
    This is a convenience method - contributors can also be updated via updateContributor(id, { received: true }).
  </action>
  <verify>
    <automated>npm test -- src/modules/state/Store.test.js --reporter=dot</automated>
  </verify>
  <done>toggleReceived method added; Store tests pass</done>
</task>

</tasks>

<verification>
- Schema migration test: Load old data (without received) and verify it defaults to false
- Selector tests: calculateRemainingNotes and calculateDistributionProgress pass all cases
- Store tests: toggleReceived works correctly
- Full test suite: npm test passes
</verification>

<success_criteria>
- Schema version is 1.1.0 with received field support
- migrateState defaults received to false for existing data
- calculateRemainingNotes filters received contributors correctly
- calculateDistributionProgress provides accurate statistics
- Store.toggleReceived convenience method works
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution/04-01-SUMMARY.md`
</output>
