---
phase: 03-display-edit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/state/selectors.js
  - src/modules/state/selectors.test.js
  - src/components/SummaryPanel.js
  - src/components/SummaryPanel.test.js
  - src/components/AppContainer.js
  - src/styles/main.css
autonomous: true
requirements:
  - SUM-01
  - SUM-02
  - SUM-03
must_haves:
  truths:
    - User can see a summary dashboard showing grand total of all contributions
    - User can see total notes needed from bank broken down by denomination
    - Summary totals update automatically when contributors are added, edited, or deleted
  artifacts:
    - path: src/modules/state/selectors.js
      provides: Bank notes and summary calculation functions
      exports: [calculateBankNotes, calculateSummary]
    - path: src/components/SummaryPanel.js
      provides: Summary dashboard component with live updates
      exports: [SummaryPanel]
    - path: src/styles/main.css
      provides: Responsive layout (sidebar on desktop, top card on mobile)
      contains: .summary-panel, .denomination-table, @media (min-width: 1024px)
  key_links:
    - from: SummaryPanel
      to: Store
      via: store.subscribe() for live updates
      pattern: store.subscribe((newState) => this.render())
    - from: calculateBankNotes
      to: contributor.breakdown
      via: aggregation across all contributors
      pattern: contributors.reduce((notes, c) => ...)
---

<objective>
Create a responsive Summary Dashboard that displays grand total contributions and bank notes needed per denomination, with live updates via Store subscription.

Purpose: Give organizers immediate visibility into total funds and exact bank note requirements for exchange.
Output: SummaryPanel component, selector functions for aggregation, responsive CSS layout
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/mnt/c/Users/force/.projects/eidiya/.planning/PROJECT.md
@/mnt/c/Users/force/.projects/eidiya/.planning/ROADMAP.md
@/mnt/c/Users/force/.projects/eidiya/.planning/phases/03-display-edit/03-CONTEXT.md
@/mnt/c/Users/force/.projects/eidiya/.planning/phases/03-display-edit/03-RESEARCH.md
@/mnt/c/Users/force/.projects/eidiya/src/modules/state/Store.js
@/mnt/c/Users/force/.projects/eidiya/src/components/AppContainer.js
@/mnt/c/Users/force/.projects/eidiya/src/modules/money/formatters.js

<interfaces>
From Store.js:
```javascript
export class Store {
  subscribe(listener) // Returns unsubscribe function
  getState() // Returns deep immutable copy of state
}
// State shape: { contributors: Array<Contributor> }
// Contributor shape: { id, name, date, amountInFils, breakdown: { five, ten, twenty, fifty, hundred, twoHundred, fiveHundred, thousand } }
```

From formatters.js:
```javascript
export function formatAED(amountInFils) // Returns formatted string like "AED 1,250.00"
```

From AppContainer.js:
```javascript
export class AppContainer {
  render() // Renders full app, needs integration with SummaryPanel
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create selector functions for aggregation</name>
  <files>src/modules/state/selectors.js, src/modules/state/selectors.test.js</files>
  <behavior>
    - Test 1: calculateBankNotes([]) returns all zero counts
    - Test 2: calculateBankNotes([{breakdown: {five: 2, ten: 1}}]) returns correct sums
    - Test 3: calculateBankNotes sums across multiple contributors
    - Test 4: calculateSummary returns grandTotalFils, contributorCount, totalNotes, notes object
    - Test 5: calculateSummary with empty array returns zeros
  </behavior>
  <action>
    Create src/modules/state/selectors.js with two exported functions:
    1. calculateBankNotes(contributors) - Aggregates denomination counts across all contributors
       - Returns: { five, ten, twenty, fifty, hundred, twoHundred, fiveHundred, thousand }
    2. calculateSummary(contributors) - Returns full summary statistics
       - Returns: { grandTotalFils, contributorCount, totalNotes, notes }

    Create unit tests in selectors.test.js covering edge cases (empty array, single contributor, multiple contributors).
    Use the existing test pattern from Store.test.js.
  </action>
  <verify>
    <automated>npm test -- selectors.test.js</automated>
  </verify>
  <done>All selector tests pass, functions correctly aggregate contributor data</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SummaryPanel component</name>
  <files>src/components/SummaryPanel.js, src/components/SummaryPanel.test.js</files>
  <behavior>
    - Test 1: SummaryPanel renders with grand total, contributor count, total notes
    - Test 2: SummaryPanel renders denomination table with counts and subtotals
    - Test 3: SummaryPanel.subscribe() registers listener with store
    - Test 4: SummaryPanel.unsubscribe() removes listener
    - Test 5: SummaryPanel.destroy() cleans up DOM and listeners
  </behavior>
  <action>
    Create src/components/SummaryPanel.js as a class-based component:
    - Constructor takes store instance
    - render() returns DOM element with:
      - Header: "Bank Summary" title, grand total (formatAED), contributor count, total notes
      - Table: Denomination | Count | Subtotal (only show denominations with count > 0)
    - subscribe() - Register with store for live updates
    - unsubscribe() - Clean up store subscription
    - destroy() - Remove from DOM and unsubscribe
    - Track previous summary to detect changes for flash animation

    Use Pico CSS classes for styling. Table should use semantic HTML (<table>, <thead>, <tbody>).
    Denominations display in descending order (1000, 500, 200, 100, 50, 20, 10, 5).
  </action>
  <verify>
    <automated>npm test -- SummaryPanel.test.js</automated>
  </verify>
  <done>SummaryPanel component renders correctly, subscribes to store updates, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Integrate SummaryPanel into AppContainer with responsive layout</name>
  <files>src/components/AppContainer.js, src/styles/main.css</files>
  <action>
    Modify AppContainer.js to integrate SummaryPanel:
    1. Import SummaryPanel and instantiate in constructor
    2. Modify render() to create responsive layout:
       - Mobile (< 1024px): SummaryPanel renders as top card, then form, then contributors list
       - Desktop (>= 1024px): Two-column grid with main content (form + list) on left, sticky SummaryPanel sidebar on right
    3. Subscribe SummaryPanel to store updates in render or constructor
    4. Clean up subscription on re-render if needed

    Add CSS to src/styles/main.css:
    - .summary-panel styles (background, padding, border-radius)
    - .denomination-table styles (full width, aligned columns)
    - .summary-panel .grand-total (large, bold, primary color)
    - @keyframes flash-update animation for live updates
    - @media (min-width: 1024px) grid layout with sticky sidebar
    - Mobile-first: default styles for < 1024px, then desktop enhancement

    Ensure SummaryPanel updates flash when values change (add .updated class briefly).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>AppContainer renders SummaryPanel in responsive layout, live updates work, CSS handles 320px/768px/1024px breakpoints</done>
</task>

</tasks>

<verification>
- [ ] Run full test suite: npm test (all green)
- [ ] Manual verification: Add contributor, verify summary updates with flash animation
- [ ] Manual verification: Check responsive layout at 320px, 768px, 1024px viewports
- [ ] Verify denomination table only shows notes with count > 0
- [ ] Verify grand total matches sum of all contributor amounts
</verification>

<success_criteria>
1. SummaryPanel displays grand total in AED format (e.g., "AED 1,250.00")
2. SummaryPanel shows contributor count and total notes count
3. Denomination table shows only non-zero denominations with correct counts and subtotals
4. Summary updates automatically when contributors are added (via Store subscription)
5. Layout is responsive: top card on mobile (< 1024px), sticky sidebar on desktop
6. Visual flash animation triggers when totals change
</success_criteria>

<output>
After completion, create `.planning/phases/03-display-edit/03-01-SUMMARY.md`
</output>
