---
phase: 03-display-edit
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/modules/state/Store.js
  - src/modules/state/Store.test.js
  - src/components/ContributorCard.js
  - src/components/ContributorCard.test.js
  - src/components/DeleteConfirmation.js
  - src/components/DeleteConfirmation.test.js
  - src/components/AppContainer.js
  - src/styles/main.css
autonomous: false
requirements:
  - CONT-03
  - CONT-04
  - UX-03
user_setup: []
must_haves:
  truths:
    - User can edit existing contributor details (name, amount, breakdown)
    - User can delete a contributor with a confirmation prompt
    - All actions provide clear visual feedback (success/error notifications)
  artifacts:
    - path: src/modules/state/Store.js
      provides: updateContributor and deleteContributor actions
      exports: [updateContributor, deleteContributor]
    - path: src/components/ContributorCard.js
      provides: Inline editing with blur validation and status feedback
      exports: [ContributorCard]
    - path: src/components/DeleteConfirmation.js
      provides: Modal dialog for delete confirmation
      exports: [DeleteConfirmation]
    - path: src/styles/main.css
      provides: Edit mode styling, status badges, dimming effect
      contains: .contributor-card.editing, .status-badge, .dimmed
  key_links:
    - from: ContributorCard
      to: Store
      via: onUpdate callback -> store.updateContributor()
      pattern: this._onUpdate(this._contributor.id, updates)
    - from: ContributorCard
      to: DeleteConfirmation
      via: onDeleteRequest callback -> confirmation.show()
      pattern: this._onDeleteRequest(this._contributor)
    - from: DeleteConfirmation
      to: Store
      via: onConfirm callback -> store.deleteContributor()
      pattern: onConfirm: (id) => store.deleteContributor(id)
---

<objective>
Implement contributor management features: inline editing with validation, delete with confirmation, and visual feedback for all actions.

Purpose: Allow organizers to correct mistakes and remove contributors, with clear feedback on action results.
Output: Store update/delete actions, ContributorCard with inline editing, DeleteConfirmation modal, status feedback system
</objective>

<execution_context>
@/home/rashid/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/mnt/c/Users/force/.projects/eidiya/.planning/PROJECT.md
@/mnt/c/Users/force/.projects/eidiya/.planning/ROADMAP.md
@/mnt/c/Users/force/.projects/eidiya/.planning/phases/03-display-edit/03-CONTEXT.md
@/mnt/c/Users/force/.projects/eidiya/.planning/phases/03-display-edit/03-RESEARCH.md
@/mnt/c/Users/force/.projects/eidiya/src/modules/state/Store.js
@/mnt/c/Users/force/.projects/eidiya/src/components/AppContainer.js
@/mnt/c/Users/force/.projects/eidiya/src/components/ContributorForm.js
@/mnt/c/Users/force/.projects/eidiya/src/modules/validation/contributor.js
@/mnt/c/Users/force/.projects/eidiya/src/modules/money/formatters.js

<interfaces>
From Store.js (to be extended):
```javascript
export class Store {
  addContributor(contributorData) // Existing pattern to follow
  // NEW: updateContributor(id, updates) -> returns updated contributor or null
  // NEW: deleteContributor(id) -> returns boolean
}
```

From contributor.js:
```javascript
export function validateContributorForm(values) // Returns { valid, errors: Map }
```

From formatters.js:
```javascript
export function formatAED(amountInFils) // Returns formatted string
```

From ContributorForm.js (pattern to follow):
```javascript
// Blur validation pattern
_validateField(fieldName) // Validates single field on blur
_displayFieldError(fieldName, message)
_clearFieldError(fieldName)
```

From 03-CONTEXT.md (locked decisions):
- Edit: Inline editing, click field to edit, auto-save on blur, dim other contributors
- Delete: Menu access (three-dot), confirmation modal with full details, no recovery
- Feedback: Inline status badges ("Saved", "Deleted"), 2 second duration
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add updateContributor and deleteContributor to Store</name>
  <files>src/modules/state/Store.js, src/modules/state/Store.test.js</files>
  <behavior>
    - Test 1: updateContributor updates existing contributor and returns updated object
    - Test 2: updateContributor returns null if contributor not found
    - Test 3: updateContributor preserves ID and unmodified fields
    - Test 4: updateContributor triggers setState and notifies subscribers
    - Test 5: deleteContributor removes contributor and returns true
    - Test 6: deleteContributor returns false if contributor not found
    - Test 7: deleteContributor triggers setState and notifies subscribers
  </behavior>
  <action>
    Extend Store.js with two new methods following the addContributor pattern:

    1. updateContributor(id, updates)
       - Find contributor by ID
       - Return null if not found
       - Merge updates with existing data (preserve ID)
       - Call setState with new contributors array
       - Return updated contributor object

    2. deleteContributor(id)
       - Find contributor by ID
       - Return false if not found
       - Filter out contributor from array
       - Call setState with filtered array
       - Return true

    Add comprehensive unit tests to Store.test.js following existing patterns.
    Ensure both methods trigger subscriber notifications and persistence.
  </action>
  <verify>
    <automated>npm test -- Store.test.js</automated>
  </verify>
  <done>Both Store methods implemented and tested, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DeleteConfirmation modal component</name>
  <files>src/components/DeleteConfirmation.js, src/components/DeleteConfirmation.test.js</files>
  <behavior>
    - Test 1: show(contributor) renders dialog with contributor details
    - Test 2: Clicking confirm triggers onConfirm callback with contributor ID
    - Test 3: Clicking cancel triggers onCancel callback
    - Test 4: Clicking backdrop closes dialog (cancels)
    - Test 5: Dialog uses native <dialog> element with showModal()
    - Test 6: destroy() removes dialog from DOM
  </behavior>
  <action>
    Create src/components/DeleteConfirmation.js:
    - Constructor takes onConfirm and onCancel callbacks
    - show(contributor) method:
      - Creates <dialog> element with Pico CSS styling
      - Shows contributor name, amount (formatAED), and denomination breakdown
      - Message format: "Delete {name} with {amount}?"
      - Shows breakdown details: "{value} AED x {count}, ..."
      - Two buttons: "Cancel" (secondary), "Delete" (contrast/danger)
      - Uses dialog.showModal() for native focus trap
      - Backdrop click cancels
    - _confirm() calls onConfirm(id) and closes
    - _cancel() calls onCancel() and closes
    - _close() removes dialog from DOM

    Use native <dialog> element per RESEARCH.md recommendation.
    Style with Pico CSS article/card pattern inside dialog.
  </action>
  <verify>
    <automated>npm test -- DeleteConfirmation.test.js</automated>
  </verify>
  <done>DeleteConfirmation component created and tested, modal displays correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ContributorCard component with inline editing</name>
  <files>src/components/ContributorCard.js, src/components/ContributorCard.test.js</files>
  <behavior>
    - Test 1: Renders contributor name, date, amount, breakdown
    - Test 2: Clicking field enters edit mode with input
    - Test 3: Blur triggers validation and saves if valid
    - Test 4: Invalid input shows error and stays in edit mode
    - Test 5: Menu button opens action menu with Edit/Delete options
    - Test 6: Status badge appears after save/delete and auto-removes
    - Test 7: onEditStart/onEditEnd callbacks notify parent of edit state
  </behavior>
  <action>
    Create src/components/ContributorCard.js as class-based component:
    - Constructor takes contributor data and callbacks:
      - onUpdate(id, updates), onDeleteRequest(contributor)
      - onEditStart(id), onEditEnd() for parent dimming control
    - render() creates card DOM with:
      - Header: name, date, three-dot menu button
      - Body: amount (clickable), breakdown display (clickable)
      - Menu: Edit, Delete options (hidden by default)
    - Inline editing:
      - _enterEditMode(field) - Replace text with input, focus it
      - _saveEdit(field, value) - Validate, call onUpdate if valid
      - Use same validation as ContributorForm (validateContributorForm)
      - Show inline error if invalid, stay in edit mode
      - Auto-save on blur, Enter key triggers blur
    - Status feedback:
      - _showStatus(message, type='success') - Shows badge for 2 seconds
      - Types: 'success' (green), 'error' (red)
      - Uses role="status" and aria-live="polite" for accessibility
    - Menu handling:
      - Toggle on three-dot click
      - Close on outside click
      - Edit option enters edit mode
      - Delete option calls onDeleteRequest

    Prevent multiple simultaneous edits with isEditing flag.
  </action>
  <verify>
    <automated>npm test -- ContributorCard.test.js</automated>
  </verify>
  <done>ContributorCard component with inline editing created and tested</done>
</task>

<task type="auto">
  <name>Task 4: Integrate ContributorCard and DeleteConfirmation into AppContainer</name>
  <files>src/components/AppContainer.js, src/styles/main.css</files>
  <action>
    Modify AppContainer.js:
    1. Import ContributorCard and DeleteConfirmation
    2. Replace _renderContributorCard() to use ContributorCard component
    3. Track currently editing contributor ID for dimming
    4. Wire up callbacks:
       - onUpdate -> store.updateContributor()
       - onDeleteRequest -> deleteConfirmation.show()
       - onEditStart -> set editingId, re-render to dim others
       - onEditEnd -> clear editingId, re-render
    5. Create DeleteConfirmation instance in constructor
       - onConfirm: (id) => { store.deleteContributor(id); show status }
       - onCancel: () => { /* no action */ }
    6. Add CSS to main.css:
       - .contributor-card.editing - Highlight border/shadow
       - .contributor-card.dimmed - Reduced opacity for non-editing cards
       - .status-badge - Inline badge styling with fade animation
       - .status-badge.success - Green background
       - .status-badge.error - Red background
       - .action-menu - Dropdown positioning and styling
       - .menu-button - Three-dot button styling
       - @keyframes fade-out for status badge removal

    Ensure editing state prevents Store re-render from disrupting focus:
    - When editingId is set, skip re-rendering that specific card
    - Or use local DOM manipulation approach per RESEARCH.md
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>AppContainer integrated with ContributorCard and DeleteConfirmation, styling complete</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete edit and delete management system</what-built>
  <how-to-verify>
    1. Open app in browser
    2. Add a contributor if none exist
    3. Test inline edit:
       - Click on contributor name to edit
       - Type new name, press Tab or click outside
       - Verify "Saved" badge appears briefly
       - Verify other cards dim during edit
    4. Test validation:
       - Clear name field, blur
       - Verify error shows and edit mode persists
       - Enter valid name, verify saves
    5. Test delete:
       - Click three-dot menu on a contributor
       - Click Delete
       - Verify confirmation modal shows with correct details
       - Click Cancel, verify modal closes, contributor remains
       - Click Delete again, confirm deletion
       - Verify "Deleted" badge appears and card removes
    6. Test responsive behavior:
       - Resize browser to mobile width
       - Verify edit and delete work at 320px+ width
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- [ ] Run full test suite: npm test (all green)
- [ ] Manual verification: Inline edit name, amount, breakdown fields
- [ ] Manual verification: Validation errors block save during edit
- [ ] Manual verification: Delete confirmation shows contributor details
- [ ] Manual verification: Cancel delete keeps contributor, confirm removes
- [ ] Manual verification: Status badges appear for 2 seconds
- [ ] Manual verification: Other cards dim when one is being edited
- [ ] Verify responsive layout works at 320px, 768px, 1024px
</verification>

<success_criteria>
1. User can click any field in contributor card to enter edit mode
2. Edit mode shows input field with current value, focused
3. Blur triggers validation - invalid values show error and stay in edit mode
4. Valid values save automatically, show "Saved" badge for 2 seconds
5. Other contributor cards dim (reduced opacity) when one is in edit mode
6. Three-dot menu opens action dropdown with Edit and Delete options
7. Delete opens confirmation modal showing contributor name, amount, breakdown
8. Cancel in modal closes without deleting
9. Confirm in modal deletes contributor, shows "Deleted" badge briefly
10. All status badges auto-dismiss after 2 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/03-display-edit/03-02-SUMMARY.md`
</output>
